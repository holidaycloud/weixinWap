// Generated by CoffeeScript 1.8.0
(function() {
  var Q, QRCode, config, request, _couponDetail, _couponUse, _createQrcode, _getCoupons, _getCustomerInfo, _getOpenid;

  request = require("request");

  config = require("./../config/config.json");

  Q = require("q");

  QRCode = require("qrcode");

  _getOpenid = function(code) {
    var deferred, url;
    deferred = Q.defer();
    url = "" + config.weixin.host + ":" + config.weixin.port + "/weixin/codeAccesstoken/" + global.ent + "?code=" + code;
    request({
      url: url,
      timeout: 3000,
      method: "GET"
    }, function(err, response, body) {
      var error, res;
      if (err) {
        return deferred.reject(err);
      } else {
        try {
          res = JSON.parse(body);
          if (res.error === 1) {
            return deferred.reject(new Error(res.errMsg));
          } else {
            return deferred.resolve(res.data);
          }
        } catch (_error) {
          error = _error;
          return deferred.reject(new Error("Parse Error"));
        }
      }
    });
    return deferred.promise;
  };

  _getCustomerInfo = function(openid) {
    var deferred, url;
    deferred = Q.defer();
    url = "" + config.inf.host + ":" + config.inf.port + "/api/customer/weixinLogin?ent=" + global.ent + "&openId=" + openid;
    request({
      url: url,
      timeout: 3000,
      method: "GET"
    }, function(err, response, body) {
      var error, res;
      if (err) {
        return deferred.reject(err);
      } else {
        try {
          res = JSON.parse(body);
          if (res.error === 1) {
            return deferred.reject(new Error(res.errMsg));
          } else {
            return deferred.resolve(res.data);
          }
        } catch (_error) {
          error = _error;
          return deferred.reject(new Error("Parse Error"));
        }
      }
    });
    return deferred.promise;
  };

  _getCoupons = function(customer) {
    var deferred, url;
    deferred = Q.defer();
    url = "" + config.inf.host + ":" + config.inf.port + "/api/coupon/customerCoupons?ent=" + global.ent + "&customer=" + customer + "&status=0";
    request({
      url: url,
      timeout: 3000,
      method: "GET"
    }, function(err, response, body) {
      var error, res;
      if (err) {
        return deferred.reject(err);
      } else {
        try {
          res = JSON.parse(body);
          if (res.error === 1) {
            return deferred.reject(new Error(res.errMsg));
          } else {
            return deferred.resolve(res.data);
          }
        } catch (_error) {
          error = _error;
          return deferred.reject(new Error("Parse Error"));
        }
      }
    });
    return deferred.promise;
  };

  _couponDetail = function(id) {
    var deferred, url;
    deferred = Q.defer();
    url = "" + config.inf.host + ":" + config.inf.port + "/api/coupon/detail?id=" + id;
    request({
      url: url,
      timeout: 3000,
      method: "GET"
    }, function(err, response, body) {
      var error, res;
      if (err) {
        return deferred.reject(err);
      } else {
        try {
          res = JSON.parse(body);
          if (res.error === 1) {
            return deferred.reject(new Error(res.errMsg));
          } else {
            return deferred.resolve(res.data);
          }
        } catch (_error) {
          error = _error;
          return deferred.reject(new Error("Parse Error"));
        }
      }
    });
    return deferred.promise;
  };

  _createQrcode = function(id) {
    var deferred;
    deferred = Q.defer();
    QRCode.toDataURI("http://test.meitrip.net/couponuse?id=" + id, function(err, url) {
      if (err) {
        return deferred.reject(err);
      } else {
        return deferred.resolve(url);
      }
    });
    return deferred.promise;
  };

  _couponUse = function(id) {
    var deferred, url;
    deferred = Q.defer();
    url = "" + config.inf.host + ":" + config.inf.port + "/api/coupon/scanUse";
    request({
      url: url,
      timeout: 3000,
      method: "POST",
      form: {
        id: id
      }
    }, function(err, response, body) {
      var error, res;
      if (err) {
        return deferred.reject(err);
      } else {
        try {
          res = JSON.parse(body);
          if (res.error === 1) {
            return deferred.reject(new Error(res.errMsg));
          } else {
            return deferred.resolve(res.data);
          }
        } catch (_error) {
          error = _error;
          return deferred.reject(new Error("Parse Error"));
        }
      }
    });
    return deferred.promise;
  };

  exports.coupons = function(req, res) {
    var code, state;
    code = req.query.code;
    state = req.query.state;
    return _getOpenid(code).then(function(openid) {
      return _getCustomerInfo(openid.openid);
    }, function(err) {
      console.log(err);
      return res.status(500).end();
    }).then(function(customer) {
      return _getCoupons(customer._id);
    }, function(err) {
      console.log(err);
      return res.status(500).end();
    }).then(function(coupons) {
      return res.render("coupons", {
        coupons: coupons
      });
    }, function(err) {
      console.log(err);
      return res.status(500).end();
    })["catch"](function(err) {
      console.log(err);
      return res.status(500).end();
    });
  };

  exports.couponDetail = function(req, res) {
    var id, url;
    id = req.query.id;
    url = "";
    return _createQrcode(id).then(function(u) {
      url = u;
      return _couponDetail(id);
    }, function(err) {
      console.log(err);
      return res.status(500).end();
    }).then(function(coupon) {
      return res.render("couponDetail", {
        coupon: coupon,
        url: url
      });
    }, function(err) {
      console.log(err);
      return res.status(500).end();
    });
  };

  exports.couponuse = function(req, res) {
    var id;
    if (req.headers["user-agent"].indexOf("MicroMessenger") > -1) {
      id = req.query.id;
      return _couponUse(id).then(function(coupon) {
        return res.render("useResult", {
          result: true,
          coupon: coupon
        });
      }, function(err) {
        return res.render("useResult", {
          result: false,
          message: err.message
        });
      });
    } else {
      return res.render("useResult", {
        result: false,
        message: "请使用微信扫一扫"
      });
    }
  };

}).call(this);
